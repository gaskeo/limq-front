export const routes = {
    index: '/',
    login: '/login',
    register: '/register',
    addChannel: '/addChannel',
    channelSettings: '/settings/:channelId',
    userSettings: '/userSettings'
}
